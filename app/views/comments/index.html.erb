<div id="songpartial">
  <%= render partial: "posts/songs", locals: {post: @post} %>
</div>

<div class="new_comment">
  <%= form_for @post.comments.build, remote: true do |f| %>
    <%= f.hidden_field :post_id %>
    <%= f.text_field :message,value: "🔥🔥🔥💯", autocomplete: :off %>
    <%= image_submit_tag("create_comment.png", id: 'create_comment') %>
  <% end %>
</div>

<div id="commentspartial">
  <%= render partial: "comments", locals: {comments: @post.comments.order(created_at: :desc)} %>
</div>