var body = document.getElementById('songpartial')
$(function() {
    $("#songpartial").html("<%= j render partial: "songs", locals: {post: @post} %>" + body.innerHTML);
    $("#post_<%=@post.id%>" ).hide();
    $("#post_<%=@post.id%>" ).slideDown();
});